PORT=5000
DATABASE_URL="file:./dev.db"
JWT_SECRET="supersecretkey_odissitech_2024"

# Google Sheets integration (service account JSON as a single-line JSON string)
# Generate a service account and download JSON credentials, then set the file contents as an env value.
# Example (PowerShell):
# $json = Get-Content path\to\service-account.json -Raw; $json | Out-File -Encoding ASCII -FilePath .env; # then copy value into GOOGLE_SERVICE_ACCOUNT_JSON
GOOGLE_SERVICE_ACCOUNT_JSON=
GOOGLE_SHEET_ID=

# If you prefer using a file path to the service account JSON, set GOOGLE_SERVICE_ACCOUNT_PATH instead
#GOOGLE_SERVICE_ACCOUNT_PATH=./service-account.json
